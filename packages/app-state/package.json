{
  "name": "@healthviewos/app-state",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@healthviewos/agent": "workspace:*",
    "@healthviewos/app-model": "workspace:*",
    "@healthviewos/schema": "workspace:*",
    "@healthviewos/workspace": "workspace:*",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
