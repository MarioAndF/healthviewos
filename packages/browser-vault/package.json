{
  "name": "@healthviewos/browser-vault",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "tsx src/browser-vault.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@healthviewos/data": "workspace:*",
    "@healthviewos/schema": "workspace:*",
    "dexie": "^4.2.1"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "fake-indexeddb": "^6.2.5",
    "tsx": "^4.20.6",
    "typescript": "~6.0.2"
  }
}
