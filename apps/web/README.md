# HealthView OS Web

React + Vite prototype for the HealthView OS personal health interface.

## Scripts

```bash
pnpm dev
pnpm build
pnpm lint
pnpm preview
```

The first mock ships a responsive app shell with desktop sidebar navigation, mobile bottom navigation, and a visual Health dashboard.
