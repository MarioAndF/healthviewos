{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Allow the main HealthView OS desktop window to use Tauri core APIs in local and Vite dev environments.",
  "windows": ["main"],
  "permissions": ["core:default"],
  "remote": {
    "urls": ["http://localhost:5174/*", "http://127.0.0.1:5174/*"]
  }
}
