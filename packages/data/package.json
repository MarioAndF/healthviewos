{
  "name": "@healthviewos/data",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm validate",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "validate": "tsx src/validate.ts"
  },
  "dependencies": {
    "@healthviewos/schema": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.20.6",
    "typescript": "~6.0.2"
  }
}
